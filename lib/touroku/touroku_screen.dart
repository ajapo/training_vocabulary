import 'package:flutter/material.dart';

class Touroku extends StatelessWidget {
  const Touroku({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('単語登録')),
    );
  }
}
